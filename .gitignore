.vercel
.env*.local

# Original photos (~1 GB). The site serves the optimised copies in
# assets/poze-web/, which are generated from these and are committed.
# Keep this folder locally as the master source; do not deploy it.
assets/Poze/

# Source archives the photos arrive in (hundreds of MB each), wherever they land
wetransfer_*.zip
**/wetransfer_*.zip

# OS and Office cruft
.DS_Store
~$*
